//Diese Classe wird nicht benutzt!!!!
/*
class Harvester extends Creep{

    harvestSource(source) {
        if(this.harvest(source) == ERR_NOT_IN_RANGE) {
            this.moveTo(source, {visualizePathStyle: {stroke: '#ffffff'}});
        }
    }
    
}

export default  Harvester

*/